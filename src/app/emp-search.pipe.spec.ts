import { EmpSearchPipe } from './emp-search.pipe';

describe('EmpSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new EmpSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
